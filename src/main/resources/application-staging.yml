logging:
  level:
    com.arenahub: INFO
    org.flywaydb: INFO

management:
  health:
    mail:
      enabled: false

arenahub:
  jwt:
    access-token-expiration-minutes: 60
    refresh-token-expiration-days: 30
